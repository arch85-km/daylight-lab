# Daylight Lab — engine validation

Generated by `npm run audit` (`test/audit.mjs`), which drives the shipped
`daylight-lab.html` in headless Chromium. Every figure below is measured, not
quoted. Re-run it after any change to the engines.

Run: 2026-09-03T19:38:35.546Z

---

## 1. Analytic benchmarks

Checks against answers derived independently of the code.

### 1.1 Unobstructed point, CIE overcast sky

A sensor with nothing above it must read exactly 100% daylight factor. The
residual is pure Monte-Carlo noise and should fall as 1/√N.

| Rays | DF (%) | Error |
|---:|---:|---:|
| 128 | 99.9606 | -0.0394% |
| 512 | 99.9827 | -0.0173% |
| 2048 | 99.9887 | -0.0113% |
| 8192 | 100.0017 | +0.0017% |
| 32768 | 99.9997 | -0.0003% |

### 1.2 Sky integrals

The discretised sky must reproduce the closed-form hemisphere integrals:
`∫L·cosθ·dω = 7π/9·L_z` for the CIE overcast sky and `π·L` for a uniform sky.

| Subdivision | Patches | Σω vs 2π | Overcast | Uniform |
|---|---:|---:|---:|---:|
| MF:1 | 145 | -1.3e-14 | 2.4488 (+0.22%) | 3.1416 (+0.00%) |
| MF:2 | 577 | -2.0e-14 | 2.4493 (+0.24%) | 3.1416 (+0.00%) |

Exact: overcast 2.44346, uniform 3.14159.

### 1.3 Rectangular roof aperture, closed form

A point directly below the centre of an a×b opening in an opaque horizontal
plane at height h, under a uniform sky. The configuration factor from a
horizontal element to a rectangle sharing a corner above it is

```
F = (1/2π) [ X/√(1+X²)·atan(Y/√(1+X²)) + Y/√(1+Y²)·atan(X/√(1+Y²)) ]
DF = 100 · 4F        X = a/2h,  Y = b/2h
```

| Opening | Height | Exact DF | Measured | Error |
|---|---:|---:|---:|---:|
| 2 × 2 m | 2 m | 23.946% | 23.947% | +0.01% |
| 4 × 3 m | 2.5 m | 37.119% | 37.114% | -0.01% |
| 1 × 1 m | 3 m | 3.411% | 3.415% | +0.12% |

### 1.4 Sealed box

A room with no openings must read exactly zero. Any non-zero value would mean
a gap in the geometry or a ray-origin epsilon leak.

- Raytraced, maximum over the grid: **0 lx**
- Split-flux, maximum over the grid: **0%**

---

## 2. Convergence and numerical behaviour

### 2.1 Monte-Carlo noise vs ray count

Five independent bakes of the default model at each ray count. The standard
deviation of the mean daylight factor is the number to read: it is the
repeatability a student should expect if they re-run the same model.

> This sweep caught a real bug. The path-termination cutoffs were absolute
> (`w < 1e-4`) while a sample starts at `w = π/N`, so raising the ray count
> shrank the starting weight and truncated interreflection after fewer
> bounces — a higher quality setting returned a *lower* daylight factor
> (9.11% at 4096 rays against 9.30% at 768). The cutoffs are now relative to
> the starting weight and the mean is flat across the range.

| Rays/point | Mean DF | SD of mean | Bake |
|---:|---:|---:|---:|
| 128 | 9.357% | ±0.0095 | 155 ms |
| 192 | 9.399% | ±0.0150 | 239 ms |
| 384 | 9.310% | ±0.0120 | 441 ms |
| 768 | 9.347% | ±0.0076 | 885 ms |
| 2048 | 9.342% | ±0.0045 | 2414 ms |
| 4096 | 9.336% | ±0.0034 | 4842 ms |

### 2.2 The shipped quality tiers

| Tier | Rays | Bounces | Sky | Mean DF | SD | Bake |
|---|---:|---:|---|---:|---:|---:|
| Preview | 192 | 1 | MF:1 | 8.635% | ±0.0271 | 118 ms |
| Standard | 768 | 3 | MF:1 | 9.349% | ±0.0090 | 909 ms |
| High | 2048 | 4 | MF:1 | 9.425% | ±0.0074 | 2823 ms |
| Fine (MF:2 sky) | 4096 | 5 | MF:2 | 9.440% | ±0.0011 | 6196 ms |

### 2.3 Interreflection convergence

Bounce limit swept in a deliberately bright room (walls and ceiling ρ = 0.85,
floor 0.6), where interreflection matters most.

| Bounces | Mean DF | Gain over previous |
|---:|---:|---:|
| 0 | 7.803% | — |
| 1 | 8.895% | +13.99% |
| 2 | 10.626% | +19.47% |
| 3 | 11.500% | +8.22% |
| 4 | 12.208% | +6.16% |
| 6 | 13.016% | +6.62% |
| 8 | 13.457% | +3.39% |

### 2.4 Sky subdivision

- MF:1 → mean DF 9.337%
- MF:2 → mean DF 9.326%
- Difference: -0.11%

### 2.5 Grid independence

| Spacing | Points | Mean DF |
|---:|---:|---:|
| 1 m | 48 | 9.362% |
| 0.5 m | 192 | 9.347% |
| 0.25 m | 713 | 9.231% |

### 2.6 Symmetry

A left–right symmetric room with symmetric openings must produce a symmetric
grid. Anything beyond the Monte-Carlo noise floor in §2.1 would indicate a
structural bug in the geometry or the sampling.

- Mean left–right discrepancy: **1.57%**
- Worst single point: **7.72%**

---

## 3. Cross-engine agreement

Mean daylight factor from each engine over the same grid. Walls are 0.10 m and
the glazing sits at the inner face except in the last row, so the reveal effect
is isolated rather than mixed into every case.

Split-flux reads systematically **a few per cent above** the raytracer even with
no shading at all — that offset is the baseline to judge the shaded rows
against, not zero. Two things in the split-flux engine were calibrated against
these cases and are worth knowing about:

- A blocked sky direction is credited to the externally reflected component at
  **half** the device reflectance, because roughly half of what a blade scatters
  heads back out to the sky. Crediting the full reflectance, as the method does
  for a ground obstruction, put a louvre bank 83% above the raytraced answer.
- The aperture integration refines from 8×8 to **16×16 samples whenever a
  shading device is present**, because a five-blade bank puts structure across
  the opening finer than an 8×8 grid resolves. It costs nothing unshaded.

A louvre bank remains the case where split-flux is least reliable. Use the
raytracer for anything with fine shading structure in it.

| Case | Raytraced | Split-flux | Split-flux vs raytraced | Split-flux time |
|---|---:|---:|---:|---:|
| Small window, thin wall | 1.29% | 1.40% | +8% | 5 ms |
| Large window, thin wall | 6.58% | 6.83% | +4% | 2 ms |
| Bilateral | 5.25% | 5.57% | +6% | 4 ms |
| Deep plan 12 m | 3.34% | 3.55% | +6% | 9 ms |
| Dark finishes | 3.21% | 3.44% | +7% | 7 ms |
| Light finishes | 4.07% | 4.42% | +8% | 4 ms |
| 0.6 m overhang | 2.51% | 2.58% | +3% | 20 ms |
| Louvre bank | 0.88% | 1.04% | +18% | 33 ms |
| Skylights only | 7.87% | 7.14% | -9% | 9 ms |
| Thick wall 0.9 m | 1.81% | 3.78% | +108% | 5 ms |

---

## 4. Known limits, measured

### 4.1 Direct-sun disc sampling

The annual loop takes **one** sample of the sun disc per hour, giving a binary
shadow with no penumbra. Compared against 16 samples over five hours of a clear
June day:

- 1 sample: 4596.2 lx mean direct
- 16 samples: 4646.9 lx mean direct
- **Difference: -1.09%**

This is the accuracy floor for ASE and Direct sun hours near a shadow edge.

### 4.2 Bounce limit in very bright rooms

Paths terminate on the bounce limit, on Russian roulette below 2% of the
starting weight, or on a hard cutoff at 0.1% of it — both thresholds relative
to `π/N`, so they behave the same at every ray count (see §2.1).

The bounce **limit** is the one that bites. In a deliberately bright room
(walls and ceiling ρ = 0.9, floor 0.7):

| Bounce limit | Mean DF | Gain |
|---:|---:|---:|
| 4 | 13.125% | — |
| 8 | 15.111% | +15.13% |
| 12 | 15.814% | +4.65% |

Going from 4 bounces to 12 adds **+20%**. The shipped tiers use 1–5 bounces, so a room with
very light surfaces is **underestimated** at the default settings. Use the Fine
tier, or read the result as a lower bound, when every surface is above ρ ≈ 0.8.
Ordinary rooms (ρ ≈ 0.5 walls) converge by 3 bounces — see §2.3.

### 4.3 Luminous efficacy

A simplified fit is used rather than the full Perez efficacy model, converting
the W/m² an EPW carries into lm/W.

| Solar altitude | Beam (lm/W) | Diffuse (lm/W) |
|---:|---:|---:|
| 10° | 75.2 | 107.2 |
| 30° | 91.0 | 107.2 |
| 50° | 99.2 | 107.2 |
| 70° | 103.6 | 107.2 |

### 4.4 Not modelled at all

- Specular or directional glazing — transmittance is diffuse-equivalent.
- External context beyond a single obstruction angle (split-flux only).
- Blind operation schedules, so sDA and ASE are indicative, not IES LM-83.
- Spectral and colour effects.
- A flat 365-day year; 29 February in an EPW is skipped.

---

## 5. Console

No page errors during the audit.

---

© Karam Al-Obaidi
